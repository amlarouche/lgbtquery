import React, { FC } from "react"
import { Box, Button, Flex, Heading, Input, Select, Text } from "@chakra-ui/react"

const MinimizeQuestionBox: FC = () => {
  return <Box margin={"32px"}></Box>
}

export default MinimizeQuestionBox
